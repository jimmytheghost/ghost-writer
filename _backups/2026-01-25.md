## Wraider Development Log – 2026-01-25

### Build Plan Created

✅ Using gemma3:27b, a high-level build plan has been created: `/Users/jimmytheghost/AI-Local-Stack/wraider/docs/wraider-build-plan.md`

❌ Tried to use VSCode + Continue using Gemma3:27b but had lots of trouble. Switched to VSCode + Cline with the openai-codex:gpt-5.2-codex integration since I am a Pro user.

- I wanted to build this project using entirely offline models and tools but it was proving to be too much trouble for a simple idealistic choice.

- The goal of the project to build without Cursor so as long as I'm doing that, the paid GPT Pro Subscription gateway to codex is a good first step.

### Phase 1 Scaffold + Editor UI

✅ Initialized the Phase 1 Vite + React scaffold in `/Users/jimmytheghost/AI-Local-Stack/wraider/src/wraider-editor`.

✅ Added the Phase 1 editor UI, styling, and shortcut placeholders:

- `/Users/jimmytheghost/AI-Local-Stack/wraider/src/wraider-editor/src/App.jsx` — Replaced the default Vite counter UI with the Wraider layout and header.
- `/Users/jimmytheghost/AI-Local-Stack/wraider/src/wraider-editor/src/components/Editor.jsx` — Added the controlled textarea editor, default copy, and keyboard shortcut placeholders.
- `/Users/jimmytheghost/AI-Local-Stack/wraider/src/wraider-editor/src/App.css` — Added minimalist layout + editor styling.
- `/Users/jimmytheghost/AI-Local-Stack/wraider/src/wraider-editor/src/index.css` — Simplified base styles for the editor UI.

✅ Added `/Users/jimmytheghost/AI-Local-Stack/wraider/launch-dev.sh` to launch the dev server from the root.

✅ Added `/Users/jimmytheghost/AI-Local-Stack/wraider/launch-dev.bat` so Windows users can double‑click to install dependencies and start the dev server.

### Windows Launcher

✅ Added `launch-dev.bat` at the project root for PC users to double‑click and launch the Vite dev server.

✅ Updated `launch-dev.bat` to pass `--open`, so the browser automatically opens after the dev server starts.

✅ Updated `launch-dev.bat` to keep the window open, start the dev server in a new terminal, wait briefly, and open http://localhost:5173/ explicitly (prevents the launcher window from disappearing on error).

✅ Added a Node.js version check to `launch-dev.bat` with a friendly message if Node is missing or below 20.19 / 22.12 (Vite requirement).

✅ Upgraded Node.js on Windows to v24.13.0 (via winget) to satisfy Vite’s Node version requirement.

✅ Added a light/dark mode toggle in `src/wraider-editor/src/App.jsx` with matching theme styles in `src/wraider-editor/src/App.css`.

✅ Added New/Save/Load buttons in `src/wraider-editor/src/App.jsx` to create a new document, download markdown files, and load `.md` files.

✅ Updated `src/wraider-editor/src/components/Editor.jsx` to accept controlled content from App and wired matching styles in `src/wraider-editor/src/App.css` for the new controls.

✅ Updated the Save flow in `src/wraider-editor/src/App.jsx` to prompt for a file name before downloading a `.md` file.

✅ Replaced the save prompt with a custom modal in `src/wraider-editor/src/App.jsx` and added matching modal styling in `src/wraider-editor/src/App.css`.

### GitHub

I have never used GitHub before for my projects.  This will be my first time.

I want to work on this project on my PC but don't know how to share updates between the computers.  That's what GitHub is for obviously. Here we go!

#### GitHub Setup + Cross-Machine Workflow (Mac ⇄ PC)

✅ Confirmed the project was **not** yet a git repository and there was **no** `.gitignore` in `/Users/jimmytheghost/AI-Local-Stack/wraider`.

✅ Created a root `.gitignore` to exclude common items:

- `node_modules`
- `.DS_Store`
- `.env*`
- `*.log`
- `dist`, `build`, `coverage`

✅ Initialized the repository and created the first commit:

- `git init`
- `git add .`
- `git commit -m "Initial commit"`

✅ Created a private GitHub repo: **jimmytheghost/wraider** (no README/.gitignore/license on GitHub).

✅ Added the GitHub remote:

- `git remote add origin git@github.com:jimmytheghost/wraider.git`

✅ Encountered SSH push error (`Permission denied (publickey)`), so we set up a new SSH key specifically for this project:

- Existing key was passphrase-protected and the passphrase was unknown.
- Generated a new key: `~/.ssh/id_ed25519_github_wraider`
- Added the public key to GitHub → Settings → SSH Keys.
- Added an SSH config entry to use the new key for `github.com`.

✅ Verified SSH authentication:

- `ssh -T git@github.com` → successful auth

✅ Pushed initial commit to GitHub:

- `git push -u origin main`

✅ Provided cross‑machine workflow guidance:

- **Mac**: commit + push at end of session
- **PC**: pull before starting work

✅ Windows (PC) notes:

- User wanted to clone to `B:` drive.
- SSH agent setup failed due to access‑denied (no admin PowerShell).
- Switched to **HTTPS** as the simplest path without admin rights.

✅ HTTPS clone instructions:

- `B:` → `cd B:\\`
- `git clone https://github.com/jimmytheghost/wraider.git`

✅ GitHub auth via browser (Git Credential Manager) explained.

✅ If needed later, provided command to switch remote to HTTPS:

- `git remote set-url origin https://github.com/jimmytheghost/wraider.git`

#### Learning GitHub

Holy smokes, I'm a n00b. But I'm doing my best here.  I'm organized the file structure a bit. I've created a `/wraider/docs/build-plans` folder. I also created a `/wraider/docs/agent-workflows` folder and `/wraider/docs/user-workflows` folder. Agent Workflows are for Agents to follow. User workflows are to help me remember how to use GitHub.

To future users, you can probably ignore this file.