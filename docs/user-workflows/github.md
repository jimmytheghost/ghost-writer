Here’s the **exact, specific commands** for this project on each machine.

---
# ✅ On your **Mac** (project lives here)
**Before you start work:**
```bash
cd /Users/jimmytheghost/AI-Local-Stack/wraider
git pull
```

**After you finish work:**
```bash
cd /Users/jimmytheghost/AI-Local-Stack/wraider
git status
git add .
git commit -m "your message"
git push
```

---
# ✅ On your **PC** (project on B: drive)
**Before you start work:**
```powershell
cd B:\wraider
git pull
```

**After you finish work:**
```powershell
cd B:\wraider
git status
git add .
git commit -m "your message"
git push
```

That’s the exact formula for this project on each machine.