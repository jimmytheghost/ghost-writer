## Wraider Development Log – 2026-01-26

### Changes
- Added `launch.command` (macOS launcher) to stop any existing process on port 5173, start the Vite dev server, and open the browser.
- Added `stop.command` (macOS launcher) to stop the Vite dev server running on port 5173.
- Updated `readme.md` with macOS launcher usage instructions for starting and stopping the app.
- Added detailed Phase 2 build plan for Ollama prompting (`docs/build-plans/wraider-02-build-plan.md`).
- Implemented Phase 2 prompting UI and Ollama integration in the editor (`App.jsx`, `Editor.jsx`, `App.css`).
- Updated prompt shortcut to Cmd+Shift+K and refreshed the editor hint.
- Updated prompt responses to replace selected text or insert at the cursor position.
- Hid the Ollama response container now that output is inserted into the document.
- Enabled streaming Ollama responses so inserted text updates live while generation runs.
- Added a stop-generation control and spinner indicator in the prompt overlay.
- Moved the prompt UI into a permanent panel below the editor with updated styling.
- Removed the editor hint, removed the prompt panel heading, and moved the model selector below the prompt input.
- Removed the Prompt and Model labels from the prompt panel.

