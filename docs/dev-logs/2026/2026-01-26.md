## Wraider Development Log – 2026-01-26

### Changes
- Added `launch.command` (macOS launcher) to stop any existing process on port 5173, start the Vite dev server, and open the browser.
- Added `stop.command` (macOS launcher) to stop the Vite dev server running on port 5173.
- Updated `readme.md` with macOS launcher usage instructions for starting and stopping the app.
- Added detailed Phase 2 build plan for Ollama prompting (`docs/build-plans/wraider-02-build-plan.md`).
- Implemented Phase 2 prompting UI and Ollama integration in the editor (`App.jsx`, `Editor.jsx`, `App.css`).
- Updated prompt shortcut to Cmd+Shift+K and refreshed the editor hint.
- Updated prompt responses to replace selected text or insert at the cursor position.
- Hid the Ollama response container now that output is inserted into the document.
- Enabled streaming Ollama responses so inserted text updates live while generation runs.
- Added a stop-generation control and spinner indicator in the prompt overlay.
- Moved the prompt UI into a permanent panel below the editor with updated styling.
- Removed the editor hint, removed the prompt panel heading, and moved the model selector below the prompt input.
- Removed the Prompt and Model labels from the prompt panel.
- Updated `readme.md` to describe the permanent prompt box and cursor/selection-based insertion behavior (with selection replacement).
- Updated `Editor.jsx` to preserve cursor/selection changes so prompt submissions can replace selections or insert at the correct cursor position.
- Updated `App.jsx` to consume live selection updates from the editor and use them for prompt insertions.
- Added a custom selection overlay so highlighted text remains visibly marked when the prompt box has focus (`Editor.jsx`, `App.jsx`, `App.css`).
- Adjusted editor overlay/textarea width styles to keep the main text container at full width (`App.css`).
- Raised the selection overlay above the textarea so the highlight remains visible even after focus moves to the prompt box (`App.css`).
- Hid the header title/subtitle so only the document controls remain visible in the top bar (`App.jsx`).
- Removed the top header and moved the document controls into a new footer bar (`App.jsx`, `App.css`).
- Restyled the prompt panel so the form container owns the border and padding, eliminating extra outer whitespace (`App.css`).
- Removed the prompt form container styling so only the prompt textarea carries a single border (`App.css`).
- Removed the drop shadow behind the main editor text area (`App.css`).
- Hid the "Generating..." status container so only the spinner shows during generation (`App.jsx`).
- Placed the model selector and prompt action buttons on the same row in the prompt panel (`App.jsx`, `App.css`).
- Removed the bottom padding under the main content so the prompt panel sits flush with the footer (`App.css`).
- Disabled flex growth on the main content container to eliminate the remaining gap above the footer (`App.css`).
- Added a small bottom padding to the main content area so there is a subtle gap above the footer (`App.css`).
- Set the footer to a 30px height and reduced footer button sizes by half (`App.css`).
- Fixed the footer to the bottom of the viewport and added matching bottom padding to the main content so it no longer leaves a blank gap (`App.css`).
- Moved the prompt panel to sit just above the fixed footer and let the editor textarea fill the remaining vertical space (`App.css`).
- Resized the editor and prompt panel to stretch to full width (up to 900px) and restored a larger minimum editor height to avoid the compressed layout (`App.css`).
- Increased the footer height to 50px, resized footer controls accordingly, and relocated the model selector into the footer (`App.jsx`, `App.css`).
- Increased the editor field/textarea minimum height so the editor fills more vertical space and reduces the gap above the prompt panel (`App.css`).
- Added a small spacing gap between the editor text area and the prompt panel (`App.css`).
- Added explicit top margin to the prompt panel to create visible spacing from the editor (`App.css`).
- Disabled page-level vertical scrolling so only the editor and prompt can scroll (`index.css`).
- Disabled resize handles on the editor and prompt textareas so the bottom-right resize arrows no longer appear (`App.css`).
- Removed the default arrow icon from the footer model dropdown (`App.css`).

