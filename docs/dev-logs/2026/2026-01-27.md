## Wraider Development Log – 2026-01-27

### Changes Planned

✅ Change light mode to grey and dark mode to shades of black.
✅ Undo generation button. Reverts text to the state before it was generated.
✅ Put Send / Stop buttons in the Prompt Panel input to preserve space.
✅ Markdown Viewer Button - when clicked, users can see the markdown preview.
❌ Different colors / beautify the markdown syntax like ### and `` etc.
    - Tried this. Making the text area contenteditable caused some errors that seemed to make the purely aesthetic feature more trouble than it is worth. But this is definitely worth revisiting in the future.
✅ A copy to clipboard button - copies all text to clipboard
    ✅ If text is selected, it should only copy only that part.
- Adjust frontend fonts for text edit / prompt panel and then Markdown preview.
✅ Replace new / save / load / light modes with Google material icons.
- Save documents as most universal file types: .md, .pdf, .doc, .docx, .yaml
    - When saving, user can choose to save the plain text version or rendered markdown.
 
### Ideas / Stretch Goals

- Options - the LLM generates two different options and the user selects which one they want.
- Make this a stand alone app and not web-based. No browser header or url bars.

### Changes Completed

- Updated `src/wraider-editor/src/index.css` root colors to a neutral light gray palette (`#f2f2f2` background, `#1f2937` text).
- Updated `src/wraider-editor/src/App.css` to replace the blue/purple dark theme with black-forward tones and refreshed light grays across app surfaces, inputs, buttons, and focus states.
  - Light palette highlights: `#f2f2f2` base, `#f7f7f7` surfaces, `#d1d1d1` borders, `#6b7280` primary action.
  - Dark palette highlights (from Alternatives to Black reference): `#0b1215` base, `#101720` surfaces, `#1b1b1b` editor, `#232023` borders, `#5b6368` focus/primary action.
- Updated `src/wraider-editor/src/App.jsx` to use a single Undo/Redo toggle button that switches its label/action between undoing and redoing the most recent generation.
- Updated `src/wraider-editor/src/App.jsx` and `src/wraider-editor/src/App.css` to place the prompt textarea beside a stacked button column (Send, Stop, Undo/Redo) aligned on the right.
- Replaced New/Save/Load/Theme text labels with Material Symbols icons in `src/wraider-editor/src/App.jsx`, added icon styling in `src/wraider-editor/src/App.css`, and loaded the Material Symbols stylesheet in `src/wraider-editor/index.html`.
- Added `marked@^12.0.2` to `src/wraider-editor/package.json` to render markdown previews in the editor UI.
- Updated `src/wraider-editor/src/App.jsx` to show the markdown preview in place of the editor when toggled on and hide the prompt panel in preview mode while keeping footer controls available.
- Updated `src/wraider-editor/src/App.css` to style the full-width preview layout and active preview toggle button states.
- Updated `src/wraider-editor/package-lock.json` to include the `marked` dependency tree.
- Updated `src/wraider-editor/src/App.jsx` to move the markdown preview title above the preview container and remove the subtitle text.
- Updated `src/wraider-editor/src/App.css` to style the new preview title placement and drop the unused preview subtitle styles.
- Updated `src/wraider-editor/src/App.jsx` copy-to-clipboard handler to prioritize active selections (DOM or editor range) before falling back to the full document content.
- Updated `src/wraider-editor/src/components/Editor.jsx` to render a markdown syntax overlay in the editor while retaining selection highlighting.
- Updated `src/wraider-editor/src/App.css` with markdown overlay/token styling to soften markdown characters in light/dark themes.
- Updated `src/wraider-editor/src/components/Editor.jsx` to use a contenteditable editor that wraps heading `#` tokens for inline styling in the editor.
- Updated `src/wraider-editor/src/App.css` to style inline `#` tokens with bold, muted colors in light/dark modes.
- Reverted `src/wraider-editor/src/components/Editor.jsx` back to the textarea-based editor after contenteditable input issues.
- Reverted `src/wraider-editor/src/App.css` to remove the inline markdown styling adjustments tied to the contenteditable editor.
- Updated `src/wraider-editor/src/App.css` to style the theme toggle button as a rounded square, matching the New/Save/Load buttons.
- Updated `src/wraider-editor/src/App.jsx` and `src/wraider-editor/src/App.css` to align the model select dropdown to the right next to the theme toggle.
