## Ghost Writer Development Log – 2026-01-27

### Changes Planned

✅ Change light mode to grey and dark mode to shades of black.
✅ Undo generation button. Reverts text to the state before it was generated.
✅ Put Send / Stop buttons in the Prompt Panel input to preserve space.
✅ Markdown Viewer Button - when clicked, users can see the markdown preview.
❌ Different colors / beautify the markdown syntax like ### and `` etc.
    - Tried this. Making the text area contenteditable caused some errors that seemed to make the purely aesthetic feature more trouble than it is worth. But this is definitely worth revisiting in the future.
✅ A copy to clipboard button - copies all text to clipboard
    ✅ If text is selected, it should only copy only that part.
✅ Replace new / save / load / light modes with Google material icons.
✅ Lists (like this one) aren't styled properly in MD preview. It reads as a single paragraph. Parser needs to recognize line breaks.
✅ The MD preview container should be a constant size no matter how much text is in it. And it should be scrollable for long text.

 
### Ideas / Stretch Goals

- Options - the LLM generates two different options and the user selects which one they want.
- Make this a stand alone app and not web-based. No browser header or url bars.
- Save documents as most universal file types: .md, .pdf, .doc, .docx, .yaml
    - When saving, user can choose to save the plain text version or rendered markdown.
- Adjust frontend fonts for text edit / prompt panel and then Markdown preview.

### Changes Completed

- Updated `src/ghost-writer-editor/src/index.css` root colors to a neutral light gray palette (`#f2f2f2` background, `#1f2937` text).
- Updated `src/ghost-writer-editor/src/App.css` to replace the blue/purple dark theme with black-forward tones and refreshed light grays across app surfaces, inputs, buttons, and focus states.
  - Light palette highlights: `#f2f2f2` base, `#f7f7f7` surfaces, `#d1d1d1` borders, `#6b7280` primary action.
  - Dark palette highlights (from Alternatives to Black reference): `#0b1215` base, `#101720` surfaces, `#1b1b1b` editor, `#232023` borders, `#5b6368` focus/primary action.
- Updated `src/ghost-writer-editor/src/App.jsx` to use a single Undo/Redo toggle button that switches its label/action between undoing and redoing the most recent generation.
- Updated `src/ghost-writer-editor/src/App.jsx` and `src/ghost-writer-editor/src/App.css` to place the prompt textarea beside a stacked button column (Send, Stop, Undo/Redo) aligned on the right.
- Replaced New/Save/Load/Theme text labels with Material Symbols icons in `src/ghost-writer-editor/src/App.jsx`, added icon styling in `src/ghost-writer-editor/src/App.css`, and loaded the Material Symbols stylesheet in `src/ghost-writer-editor/index.html`.
- Added `marked@^12.0.2` to `src/ghost-writer-editor/package.json` to render markdown previews in the editor UI.
- Updated `src/ghost-writer-editor/src/App.jsx` to show the markdown preview in place of the editor when toggled on and hide the prompt panel in preview mode while keeping footer controls available.
- Updated `src/ghost-writer-editor/src/App.css` to style the full-width preview layout and active preview toggle button states.
- Updated `src/ghost-writer-editor/package-lock.json` to include the `marked` dependency tree.
- Updated `src/ghost-writer-editor/src/App.jsx` to move the markdown preview title above the preview container and remove the subtitle text.
- Updated `src/ghost-writer-editor/src/App.css` to style the new preview title placement and drop the unused preview subtitle styles.
- Updated `src/ghost-writer-editor/src/App.jsx` copy-to-clipboard handler to prioritize active selections (DOM or editor range) before falling back to the full document content.
- Updated `src/ghost-writer-editor/src/components/Editor.jsx` to render a markdown syntax overlay in the editor while retaining selection highlighting.
- Updated `src/ghost-writer-editor/src/App.css` with markdown overlay/token styling to soften markdown characters in light/dark themes.
- Updated `src/ghost-writer-editor/src/components/Editor.jsx` to use a contenteditable editor that wraps heading `#` tokens for inline styling in the editor.
- Updated `src/ghost-writer-editor/src/App.css` to style inline `#` tokens with bold, muted colors in light/dark modes.
- Reverted `src/ghost-writer-editor/src/components/Editor.jsx` back to the textarea-based editor after contenteditable input issues.
- Reverted `src/ghost-writer-editor/src/App.css` to remove the inline markdown styling adjustments tied to the contenteditable editor.
- Updated `src/ghost-writer-editor/src/App.css` to style the theme toggle button as a rounded square, matching the New/Save/Load buttons.
- Updated `src/ghost-writer-editor/src/App.jsx` and `src/ghost-writer-editor/src/App.css` to align the model select dropdown to the right next to the theme toggle.
- Updated `src/ghost-writer-editor/src/App.css` to keep the markdown preview container at a fixed height with scrollable content for long documents.
    - Note - This did not work. I believe the agent who did this made an error.
- Updated `src/ghost-writer-editor/src/App.jsx` to configure the markdown renderer with GitHub-flavored Markdown and soft line breaks so list line breaks render properly.
- Updated `src/ghost-writer-editor/src/App.css` to improve markdown preview spacing for paragraphs and lists, and to ensure the preview content area scrolls correctly within its fixed-height container.
- Updated `src/ghost-writer-editor/src/App.css` to reinforce the markdown preview fixed-height layout by tightening flex/min-height settings on the preview container and its parent wrappers so long documents scroll reliably.
- Updated `src/ghost-writer-editor/src/App.css` to make the preview container taller by computing its height to sit just above the fixed footer while retaining scrollable content.
- Reverted `src/ghost-writer-editor/src/App.jsx` markdown indentation normalization after it failed to improve nested list rendering in the preview.
- Updated `src/ghost-writer-editor/src/components/Editor.jsx` to size the selection overlay to the textarea scroll height so persistent selection highlights work on long, scrolled documents.
- Updated `src/ghost-writer-editor/src/App.css` to clip the selection overlay inside the editor field so persistent highlights do not bleed into adjacent panels.
- ~~Updated~~ `src/ghost-writer-editor/src/App.css` to ensure the selection overlay uses border-box sizing so padding no longer pushes highlights past the bottom edge.
  -  Note from User: The problem remains but it's not a big deal for now. Will come back to it.