# 2026-03-15

## Current status

- Implemented the duplicate-file guard from the closed PR backlog so opening an already-open markdown file now surfaces a “This file is already open” modal and switches to the existing tab without spawning another copy. Keyboard shortcuts and modal escape behavior were wired into `AppModals`.
- Updated the print/export helper to wrap the rendered markdown into `ghost-writer-print-section` sections with page-break-safe styles, supporting the previously closed section-aware pagination PRs.
- Extended `renderMarkdownToSafeHtml` so standalone checkbox markers (`[ ]` / `[x]`) with no label now become preview buttons and carry the expected `data-preview-checkbox`/`aria-pressed` attributes.
- Added Vitest coverage for these flows (`App.desktop-save.test.jsx`, `src/lib/print.test.js`, and `src/lib/markdown.test.js`) and verified they all pass locally.
- Added the missing dependencies (`setSelectionRangesByTab`, `setScrollPositionsByTab`, `setPreviewOpenByTab`, and `setFooterActionFeedback`) to the `handleLoadClick` `useCallback`, aligning the handler with ESLint expectations and keeping the generated tab state updates in sync.
- Wrapped `switchActiveTab` in `useCallback`, ensuring the action stays stable for the keyboard shortcuts/modal workflow, and wired `alreadyOpenTab`/`handleConfirmAlreadyOpen` into `AppModals` so duplicate-file guard dialogs can switch to the preexisting tab.
- Updated `src/ghost-writer-editor/src/App.shortcut.test.jsx` to reflect the current single “Switch to Untitled” tab after closing all tabs, keeping the suite aligned with the runtime behavior.
- Re-running `npm run check` now passes (see Tests below), so the lint/test/build gate is green in this checkpoint.

## Blockers

- No functional blockers remain; `npm run check` now succeeds. The suite still logs the familiar `--localstorage-file` warnings, the React `act(...)` advisories from `App.windows-selection.test.jsx`, and the intentional `macos_input.prepare_failed` diagnostic emitted by `desktopRuntime.test.js`, but none fail the run.

## Tests

- `npm run check` (lint, Vitest, build) — pass with the existing noise noted above (`--localstorage-file` warnings, `act(...)` hints in `App.windows-selection.test.jsx`, and the diagnostic error printed by `desktopRuntime.test.js`).

## Outstanding work

- Draft the aggregate PR that bundles these red-pr fixes and this log once the repo is clean and the warning noise is acceptable.
