# Ghost Writer To Dos #2

[ ] In the main editor: Indented tabs should delete to the start of line when user types “cmd + delete/backspace”
-  

[ ] When a user types - at the start of the line (or indented) and hits space, it auto spaces twice
    - Similar to how it auto-formats when an indented tab is returned and a new indented tab is created with proper spacing and formatting.

[ ] Make - and tabs “bullet proof.” Make it so if a user types “Return > Tab > Tab > - “ it makes a perfect list point.

[ ] Update the Spell Check and grammar check to not be so sensitive.

[ ] In text edit mode - not markdown preview - color the text (not blue / orange?) in the ``` ``` 